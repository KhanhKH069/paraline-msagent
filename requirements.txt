# Vietnamese Bert-VITS2 Requirements
# Python 3.10+

# Deep Learning
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# PhoBERT / Transformers
transformers>=4.35.0
tokenizers>=0.15.0

# Vietnamese NLP
py_vncorenlp>=0.1.4      # VnCoreNLP wrapper
underthesea>=6.8.0        # Vietnamese NLP toolkit (backup tokenizer)

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0
audioread>=3.0.0
praat-parselmouth>=0.4.3  # Pitch analysis

# Audio Slicing
audio-slicer @ git+https://github.com/openvpi/audio-slicer.git

# Synthetic Dataset Generation
gtts>=2.5.0              # Google Text-to-Speech (tạo synthetic audio)
pydub>=0.25.1            # Audio convert mp3→wav (cần ffmpeg trong PATH)

# Training Utilities
numpy>=1.24.0
tensorboard>=2.14.0
tqdm>=4.66.0
einops>=0.7.0

# Config & Utils
omegaconf>=2.3.0
pyyaml>=6.0.1
jsonargparse>=4.27.0

# Monitoring
matplotlib>=3.7.0
seealso>=0.1.0

# Text Processing
regex>=2023.8.8
unicodedata2>=15.0.0
num2words>=0.5.13         # Số → chữ tiếng Việt

# Evaluation
jiwer>=3.0.3              # CER/WER calculation

# API / Inference Server (optional)
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
